This directory contains a submodule for each group's analysis.

Each directory can contain a directory called `macro_data` with json files that contain macro information. 
Reference `/macro_data/ExampleModel.json` for examples on how to format the json files. 
